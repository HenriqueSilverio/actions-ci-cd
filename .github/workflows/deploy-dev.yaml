name: Deploy Develop

on:
  push:
    branches:
      - main

jobs:
  deploy-dev:
    name: Deploy Develop
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: main
          fetch-depth: 0
